# Workspace

This is the agent's working directory. All files the agent reads, creates, and edits live here.

The agent can:
- Read any `.md` or `.txt` file in this directory
- Create new files
- Edit existing files surgically
- Search across all files

Add your project files to this directory to let the agent work on them.
